# 07 — MCP, hooks, plugins, protocol

Structurally present and matching the design; most need live conformance runs that the TUI outage partly blocked. Lower-confidence findings, tagged accordingly.

## Positive confirmations — **[code]**

- **Deferred MCP loading is real.** `rw-mcp/src/manager.rs:336` `deferred_tool_index()` returns name+one-liner entries; `defer_tools: true` default (`client.rs:652`); `deferred_prompt()` exists for the "<2k tokens until first use" measurement.
- **Hook dispatcher with fail-open/closed semantics** (`rw-ext/src/hook.rs:359`), tested for fail-open-continues and fail-closed-stops. The M2 internal-dispatcher / M8 RPC-bridge split appears honored.
- **Protocol codegen is Rust-first and drift-checked.** `protocol/types.ts` is `@generated by cargo xtask codegen`; the TUI re-exports generated types (ADR-013). `bun run typecheck` passes; `bun test` 132/132 incl. SSE transport perf (p99 ≈ 1.2 ms).
- Driver-lease (`TakeDriver`), `UserShellStarted/Ended`, `ToolOutputPruned`, `UserShellStateChanged` all exist in the wire types — the round-2 contracts landed.

## GAP-07-01 — `hooks.toml` declarative shell-hook tier presence unverified — **P2 [code]**

Dispatcher and `[toolchain]` confirmed; a `hooks.toml` loader wired into discovery was not. Verify a `[[hook]] event="post_tool" run="…"` fixture fires, and that `rw import` produces it from Claude Code settings-hooks.

## GAP-07-02 — Live MCP conformance not run — **P2 [code]**

Deferred-loading token budget, capability-violation kill, and Rottweiler-as-MCP-server driven by another agent (M8 ACs) not exercised. Scaffolding present; run the fixtures against a real stdio server.

## GAP-07-03 — Plugin host / capability approval not exercised — **P2 [code]**

`rw plugin scaffold/dev` exist and `plugin_runtime.rs` is substantial; the capability-manifest approval flow and "manifest omits `network` → killed on outbound" security test were not reproduced live.
