source_url = "https://models.dev/api.json"
snapshot_date = "2026-07-10"
revision = "models.dev-2026-07-10-bootstrap"

# Small bootstrap snapshot; refreshed data is installed through the parser and
# atomic replacement seam instead of adding model ids to Rust source.
[models."openai/gpt-5-mini"]
display_name = "GPT-5 Mini"
max_context_tokens = 400000
max_output_tokens = 128000
supports_tools = true
supports_thinking = true
input_per_million_micros_usd = 250000
output_per_million_micros_usd = 2000000
cache_read_per_million_micros_usd = 25000
cache_write_per_million_micros_usd = 0

[models."anthropic/claude-haiku-4-5"]
display_name = "Claude Haiku 4.5"
max_context_tokens = 200000
max_output_tokens = 64000
supports_tools = true
supports_thinking = true
input_per_million_micros_usd = 1000000
output_per_million_micros_usd = 5000000
cache_read_per_million_micros_usd = 100000
cache_write_per_million_micros_usd = 1250000
