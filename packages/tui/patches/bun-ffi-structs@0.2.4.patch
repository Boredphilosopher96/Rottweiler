diff --git a/dist/index.js b/dist/index.js
index 8d4cd1cd51d1a46201e25ac28628e32a7b3fd5e2..95080303e2a7dc6c1ec0342cc5cdb80e575d2800 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,6 +1,8 @@
+import { ptr as bunPtr, toArrayBuffer as bunToArrayBuffer } from "bun:ffi";
+
 // src/ffi.ts
 var FFI_LOAD_ERROR = "bun-ffi-structs requires Bun or Node.js with node:ffi enabled (--experimental-ffi --allow-ffi).";
-var backend = await loadBackend();
+var backend = createBunBackend({ ptr: bunPtr, toArrayBuffer: bunToArrayBuffer });
 function unavailable(cause) {
   throw new Error(FFI_LOAD_ERROR, {
     cause: cause instanceof Error ? cause : undefined
