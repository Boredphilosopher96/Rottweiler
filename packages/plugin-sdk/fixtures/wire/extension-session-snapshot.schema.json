{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExtensionSessionSnapshot",
  "description": "Bounded operational state; prompts, credentials and provider configuration\nare outside this session observation capability.",
  "type": "object",
  "properties": {
    "session_id": {
      "$ref": "#/$defs/SessionId"
    },
    "title": {
      "type": [
        "string",
        "null"
      ]
    },
    "mode_id": {
      "$ref": "#/$defs/ModeId"
    },
    "model_alias": {
      "type": "string"
    },
    "active_turn": {
      "anyOf": [
        {
          "$ref": "#/$defs/TurnId"
        },
        {
          "type": "null"
        }
      ]
    },
    "queued_messages": {
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "last_sequence": {
      "anyOf": [
        {
          "$ref": "#/$defs/SequenceId"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "session_id",
    "mode_id",
    "model_alias",
    "queued_messages"
  ],
  "$comment": "// @generated by cargo xtask codegen; do not edit by hand.",
  "$defs": {
    "SessionId": {
      "description": "Stable identifier of an engine session.",
      "type": "string"
    },
    "ModeId": {
      "description": "Open identifier of a built-in or extension-provided mode.",
      "type": "string"
    },
    "TurnId": {
      "description": "Stable identifier of a conversation turn.",
      "type": "string"
    },
    "SequenceId": {
      "description": "Monotonic per-session sequence encoded as a decimal string on the wire.",
      "type": "string"
    }
  }
}
